{
  "type":"commonjs",
  "scripts": {
    "migrate": "NODE_ENV=production npx sequelize-cli db:migrate",
    "migrate-down-last": "npx sequelize-cli db:migrate:undo",
    "migrate-down-all": "npx sequelize-cli db:migrate:undo:all"
  }
}